import { FC } from 'react';

const Navbar: FC = () => {
  return (
    <div className="dark:bg-gray-800 dark:text-white py-2">
      <nav className="container">Elanum</nav>
    </div>
  );
};

export default Navbar;
